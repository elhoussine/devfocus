import { connect } from "react-redux";
import JobsCreate from './JobsCreate'

const mapStateToProps = state => {
  return {

  }
}

const mapDispatchToProps = dispatch => {
  return {

  }
}

export default connect(null, null)(JobsCreate) //fix nulls later